{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.libelle) }}
    </div>
    <div class="form-group">
        {{ form_row(form.statut) }}
    </div>
    {{ form_widget(form) }}
    <div class="form-group text-right">
        <button class="btn btn-success">{{ button_label|default('form.btnSave'|trans) }}</button>
    </div>

{{ form_end(form) }}
