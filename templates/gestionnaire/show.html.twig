{% extends 'base.html.twig' %}

{% block title %}Gestionnaire{% endblock %}

{% block body %}
    <h1>Gestionnaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gestionnaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ gestionnaire.nom }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ gestionnaire.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gestionnaire_index') }}">back to list</a>

    <a href="{{ path('gestionnaire_edit', {'id': gestionnaire.id}) }}">edit</a>

    {{ include('gestionnaire/_delete_form.html.twig') }}
{% endblock %}
