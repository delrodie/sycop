{% extends 'base.html.twig' %}

{% block title %}District{% endblock %}

{% block body %}
    <h1>District</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ district.nom }}</td>
            </tr>
            <tr>
                <th>Localite</th>
                <td>{{ district.localite }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ district.user }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ district.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('district_index') }}">back to list</a>

    <a href="{{ path('district_edit', {'id': district.id}) }}">edit</a>

    {{ include('district/_delete_form.html.twig') }}
{% endblock %}
